post_install() {
  echo "Reloading udev rules..."
  udevadm control --reload-rules
  udevadm trigger -s usb -s hidraw -c add -a idVendor=1532
  echo "Enable the user service with:"
  echo "  systemctl --user enable --now uchromad.service"
}

post_upgrade() {
  post_install
}
